QJM Steps
=========
1. Setup zookeeper quorum and make sure they are running
	- ./zookeeper-3.4.5/bin/zkServer.sh start
  	- ./zookeeper-3.4.5/bin/zkCli.sh
  	- ./zookeeper-3.4.5/bin/zkCli.sh -server nn1.cluster1.com:2181

2. Setup core and hdfs file on each node as given.

3. Start Journalnode on all journal node machines
	- hadoop-daemon.sh start journalnode

4. Format namenode (Do not run this command, if you already have a NN runing and updating it to HA)
	- hdfs namenode -format

5. Initialize shared edits for Journal node to see: (namenode must be stopped for this step;This statement is valid if step 4 is not executed)
        - hdfs namenode -initializeSharedEdits -force

6. Format zkFC
	- hdfs zkfc -formatZK

7. Start namenode
	- hadoop-daemon.sh start namenode

8. Start ZKFC
	- hadoop-daemon.sh start zkfc

9. Bootstrap StandbyNamenode
	- hdfs namenode -bootstrapStandby

10. Start Namenode on standby

11 .Start ZKFC on standy

Now you cluster is HA with one active Namenode
